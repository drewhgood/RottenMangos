<h1>Users</h1>
<hr />
<% @users.each do |user| %>
  <h2><%= user.firstname %> <%= user.lastname %></h2>
  <%=button_to "Delete", admin_user_path(user.id), :method => :delete, :class => "delete", :confirm => "Are you sure ?"%>
<% end %>

<%= paginate @users %>
<%= page_entries_info @users %>
<hr />